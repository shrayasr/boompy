from setuptools import setup

setup(name='boompy',
      version='0.1',
      description='Clone of Holman\'s BOOM in Python',
      license='MIT',
      packages=['boompy'],
      scripts=['bin/boom'],
      zip_safe=False)
